# PriorityClass по умолчанию: все поды без явного priorityClassName
# получают высокий приоритет 1000000.
apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: high-priority-default
value: 1000000
globalDefault: true # Все поды без указанного priorityClassName получат этот класс
description: "Приоритет по умолчанию для всех обычных подов"
---
# PriorityClass для специализированных подов: значение меньше дефолтного,
# поэтому при нехватке ресурсов именно эти поды будут вытеснены первыми.
apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: low-priority-specialized
value: 1000 # Меньше, чем у дефолтного класса
globalDefault: false
description: "Низкий приоритет для специализированных подов"
preemptionPolicy: PreemptLowerPriority # Значение по умолчанию: высокий вытесняет низкий
